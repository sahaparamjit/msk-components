// Export the component in this file to display in the preview

import metadata from './metadata'
import OpenPara from './openpara'

const components = {
    OpenPara
}

const Cover = {
  metadata,
  components
}

export default Cover;